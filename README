# WUBAC

To run this project locally, you have to go into the project directory in CMD and type 'npm install' it will download all the node_modules.

Once its completed type 'npm run dev' in CMD.

Successful build will show you the http://localhost:3000/.
